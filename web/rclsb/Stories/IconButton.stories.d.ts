import { StoryObj } from '@storybook/react';
import { default as IconButton } from '../Buttons/IconButton';
declare const meta: {
    title: string;
    component: typeof IconButton;
    tags: string[];
};
export default meta;
type Story = StoryObj<typeof meta>;
export declare const Default: Story;
